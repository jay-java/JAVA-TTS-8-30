server:
  port: 8083

spring:
  application:
    name: shop-agent-service
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:demo} # Default to 'demo' or read from env
      chat:
        options:
          model: gpt-3.5-turbo

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
